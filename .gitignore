# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv*/
venv/

# WaveDB runtime data
data/memory_db/
data/test_corpus/
data/corpora/
data/pod_runs/*_extractions.jsonl
data/pod_runs/memory_db/
data/pod_runs/phase1b_scale/
data/pod_runs/phase1c_*/
.checkpoint.json
*.wal
manifest.dat

# Phase 1b scale-ingest / vector index artifacts
vector_index.faiss
vector_index_ids.json
vector_index_vectors.json

# Phase 1d Oracle-generated training data (regenerable via scripts/generate_*_training_data.py)
data/training/
.oracle_cache.json
*_checkpoint.json

# Phase 2a backbone pre-training checkpoints (regenerable via scripts/train_backbone.py / pretrain_backbone)
checkpoints/

# RunPod verbose-ssh log captures (local only)
vp_stdout.txt
vp_stderr.txt
*.log

# Notebooks
.ipynb_checkpoints/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Models (downloaded into the RunPod pod, never committed).
# models/* excludes weights + subdirs but re-includes the human-readable manifest.
*.gguf
models/*
!models/manifest.md

# Env
.env
.env.local

# Editor
.idea/

# Claude Code local settings (project-shared config would be settings.json)
.claude/settings.local.json